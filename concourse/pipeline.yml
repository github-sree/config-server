---
resources:
  - name: resource-tutorial
    type: git
    source:
      uri: https://github.com/github-sree/config-server.git
      branch: dev
jobs:
  - name: job-test-app
    public: true
    version: every
    plan:
      - get: resource-tutorial
        trigger: true
        version: every
      - task: web-app-tests
        file: resource-tutorial/concourse/tasks/build.yml